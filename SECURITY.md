# SECURITY & PRIVACY

This application is fully transparent and safe:

âœ… No network access  
âœ… No external process execution  
âœ… No Registry or system folder access  
âœ… Only reads XML + renames folders in the user-selected directory  
âœ… No code obfuscation or packing  

Users can verify all actions in the GUI log panel.
